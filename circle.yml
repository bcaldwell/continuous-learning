general:
  branches:
    only:
      - master
checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive --remote # use submodules
    - gem install htmlbeautifier

test:
  override:
    - git fetch
    - git config --global user.email "bot@bcaldwell.ca" && git config --global user.name "Benjamin Bot"

    # Publish the Github Pages Site
    - mv jekyll/* .
    - rm -rf jekyll
    - ruby ./bin/toc.rb
    - git status
    - git add --all .
    - git commit -m 'Build jekyll site'
    - git remote add gitlab git@gitlab.caldwellbenjam.in:benjamin/continuous-learning.git
    - ssh-agent bash -c 'ssh-add ~/.ssh/id_circleci_github; git push gitlab HEAD:gh-pages -f'
